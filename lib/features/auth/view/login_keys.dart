/// Stable accessibility identifiers for the Login page.
///
/// Applied with `TestId` — see `lib/core/ui/test_id.dart`. ADR-0015.
class LoginKeys {
  LoginKeys._();

  static const emailField = 'login_email_field';
  static const continueButton = 'login_continue_button';
  static const emailError = 'login_email_error';
}
